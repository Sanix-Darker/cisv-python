name: CI

on:
  push:
  pull_request:

jobs:
  version-sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0
      - name: Update cisv-core submodule to latest main
        run: git submodule update --init --remote --recursive
      - name: Verify VERSION matches pyproject versions
        run: |
          set -euo pipefail
          VERSION="$(cat VERSION)"
          PY_NB_VERSION="$(sed -n 's/^version = "\([^"]*\)"/\1/p' bindings/python-nanobind/pyproject.toml | head -n1)"
          PY_CTYPES_VERSION="$(sed -n 's/^version = "\([^"]*\)"/\1/p' bindings/python/pyproject.toml | head -n1)"
          [ "$VERSION" = "$PY_NB_VERSION" ] || {
            echo "Version mismatch nanobind: VERSION=$VERSION pyproject=$PY_NB_VERSION"
            exit 1
          }
          [ "$VERSION" = "$PY_CTYPES_VERSION" ] || {
            echo "Version mismatch ctypes: VERSION=$VERSION pyproject=$PY_CTYPES_VERSION"
            exit 1
          }

  python:
    runs-on: ubuntu-latest
    needs: version-sync
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.10', '3.12']
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0
      - name: Update cisv-core submodule to latest main
        run: git submodule update --init --remote --recursive
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Build core
        run: make -C core all
      - name: Install and test nanobind binding
        run: |
          cd bindings/python-nanobind
          pip install -U pip
          pip install .
          cd /tmp
          python - <<'PY'
          import cisv
          rows = cisv.parse_string("a,b\n1,2\n")
          assert rows[0] == ["a", "b"]
          assert rows[1] == ["1", "2"]
          print(cisv.__version__)
          PY
